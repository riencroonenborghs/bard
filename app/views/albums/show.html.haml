- content_for :meta_title do
  \- Artists
  \- #{@album.artist.name}
  \- Album
  \- #{@album.title}
  

.p-16
  .text-5xl
    = link_to artist_path(@album.artist) do
      = @album.artist.name
  .text-3xl
    = @album.title

  .pt-8.grid.grid-cols-1.gap-4
    - @album.tracks.each do |track|
      = link_to "album_path(album)" do
        .p-6.shadow-md.bg-sky-50.flex.flex-col
          .text-2xl
            = track.title
